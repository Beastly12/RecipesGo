To create a new recipe:
  POST /recipes
	JSON:
		name: string
		imageKey: string
		description: string
		ingredients: []string
		preparationTime: int

To get all recipes:
	GET /recipes
	To get next page:
		GET /recipes?last={id of last recipe received}

To delete a recipe:
	DELETE /recipes/{recipe_id}

To add recipe to favorite:
	POST /favorites
	JSON:
		recipeId: string

To remove recipe from favorite:
	DELETE /favorites/{recipe_id}
	
To get all favorites recipes:
	GET /favorites

To get url to post pictures to:
	GET /upload-url