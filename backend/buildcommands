# add main.go files to build in the build dirs
docker build --build-arg BUILD_DIRS="pre_signup post_signup" -t recipe_go .
docker create --name extract-builder recipe_go
rm -rf ./bin
docker cp extract-builder:/app/bin ./bin
docker rm extract-builder